class PriorityQueue {
   constructor() {
    //initializing queue
    this.collection = [];
  }

  printCollection() {
    console.log(this.collection);
  }
    // Only change code below this line
  enqueue(element){
    
    return this.collection.push(element)
  }

  findPriority(){

    var values = array.map(function(keys) { return keys[1]; });
    var thePriority = Math.min.apply(null, values)
   return array.find(element => element[1] == thePriority)

   
  }
  dequeue(){
    //shift is like pop but removes elements from the beginning of the array
    return this.collection.shift()
  }
 
  size(){
    return this.collection.length
  }

  // returns if the queue is empty
  isEmpty() {
    return this.size() === 0;
  }

}
var array = new PriorityQueue();
array.enqueue(['human', 1])

 //array.test();
/*
function arrayRemove(arr) {
  var values = array.map(function(keys) { return keys[1]; });
  var thePriority = Math.min.apply(null, values)

   return arr.filter(function(element){
       return element[1] != thePriority;
   });

}

var result = arrayRemove(array);
//console.log(result)
*/
